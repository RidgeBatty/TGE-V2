import { Flipbook } from "../flipbook.js"

export class AnimationEditor extends Flipbook {
    constructor(o) {
        super(o);
    }

    convertFramesToAtlas(sequences, dims, order = 'left-to-right') {

    }
}